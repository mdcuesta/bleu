using System.Web.Mvc;

namespace Bleu.Mvc
{
    public class BleuController : Controller
    {
        #region Commented
        //protected virtual ViewResult ArticleView(int year, int month, int day, string title)
        //{
        //    var viewName = string.Format("{0}-{1:d2}-{2:d2}-{3}", year, month, day, title);
        //    return View(viewName);
        //}
        #endregion
    }
}
